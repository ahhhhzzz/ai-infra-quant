# TASK-006B-Q-R03 — 确认与滚动稳定性研究报告

状态：**数学规格、最小模型与合成验证已完成，等待独立审查。**

建议明确区分“当前窗口可重建的确认凭证”与“在某 cutoff 实际识别过的历史记录”。
有限局部证据可以在明确条件下证明滚动一致性，但代价是改变极值、波动尺度和候选域语义；
记录历史可以保证旧记录不被改写，但必须公开额外状态，不能宣称是无状态窗口算法。
本轮不接受 H1 为稳定替代方案，也不把数学条件成立写为产品或真实行情通过。

## 1. 精确依据与执行顺序

| 对象 | SHA |
|---|---|
| 产品权威，保持不变 | `8bf55f1ea5e7b3e9e65517ca9b5a505086bb761f` |
| 开发基线，465 个文件全部保护 | `ddb61c15f1aea026b26c116f35fa1e2b0b0d055a` |
| R03 合同与开工 HEAD | `8338144f0185f1b5b416daf08d5bcbff074bb345` |
| 精确 R02 独立审查 | `c01c69d7f3a8b6cc21d0dd4f6773cf14308841e4` |
| 模型／命题／域冻结提交 | `2e048607414021e12984871db3a888cba7f20557` |
| 最小模型、复现器与专用测试提交 | `ea46324471c6c558afa66896a1b95eaadda2f44e` |

Fetch 核实 R03 远程 HEAD 等于合同提交，唯一父提交为开发基线；R02 审查是开发基线的独立子提交，
直接读其 Git 对象，未合并 review 分支。产品权威仍在上述精确版本。使用独立
`D:/AI_Infra_Quant_Codex_v1/task006b-q-r03-worktree`，其他 worktree、未跟踪文件及用户数据库未操作。
AGENTS、治理文档、适用历史 phase plan、原 Q 合同／数学、F01/F02 合同／报告、R02 冻结计划／数学／
报告和精确审查均已阅读；此前已读的治理文件与开发基线保持同一 blob，未把历史停止指令当成重启任务。

顺序：核验 → 阅读已审根因和拒绝证据 → 提交冻结 PLAN → 实现两个最小语义模型 →
独立手算回归及有界枚举 → 完整数学证明／代价比较 → 测试、静态与保护核验 → 本报告。
未根据枚举结果调整模型、阈值、样本或冻结计划。中间只修正新增代码的类型／格式；报告尚未创建时，
链接检查曾正确指出缺失 REPORT，交付时补齐并重新核验。冻结计划始终不变。
最终交付提交包含本报告及证据；其 SHA 在提交后由 GitHub 读回返回，避免在报告内嵌入自引用哈希。

## 2. 交付内容与数学结论

[完整数学规格](../../../research/PAQS_Q_CONFIRMATION_AND_ROLLING_STABILITY_R03.md)定义 I_t、
B_t、warm/active、F_theta 与独立历史对象，区分极值、反转完成、证据可用、首次实际识别和计算时间。
确认依赖包含负向候选证据和 ATR seed／先前状态，不能用两个端点引用代替完整因果支持。

- **M1 / R03-LOCAL-1**：四条连续行情上的有限局部确认凭证。严格相邻极值、前一 raw 转折否决、
  XOR 歧义与两条极值间隔；使用前一条 high-low 作为单位反转尺度，明确不再是原 EMA ATR。
  实现只处理最小 N=8，W=0 与最低支持 padding W=2 是两个视图，不是行情参数候选网格。
  不保证高低交替，不生成 Swing、Zone、Range、Regime 或趋势输出。
- **M2 / R03-RECORD-1**：只在内存中累积 M1 首次识别记录；显式空初态、严格递增 cutoff、lineage、
  不可变 tuple。旧记录不改写，当前支持状态单列；不同历史的输出可以不同，内存随历史增长。
  不是持久化 Pivot ledger，也没有接入产品。

有效手算 HIGH110/LOW90 在确认条目完成后可识别；双向、等极值、flat、单调、零尺度不强制输出。
首次调用在 cutoff7 识别确认于条目3的事件时，首次识别时间记录为 cutoff7；延迟可用同样不回填历史知识。
合法 PARTIAL/UNKNOWN 不升级，模型进一步保守要求 COMPLETE AS_OF 连续等时长条目；这项额外限制会拒绝
真实 session gap，尚无日历语义采纳。原 F01/F02 校验、原型、策略参数和测试全部保留。

### 证明／反例登记

各命题的量词、前提和证明正文见规格第5节；以下状态不是“数学所有性质已通过”。

| 命题 | 状态 | 核心证据／限制 |
|---|---|---|
| P1 固定 cutoff 因果隔离 | PROVED_UNDER_ASSUMPTIONS | 原始版本保留、合法时间外壳及输入界限内，未来版本先隔离；每个模型使用的全部8条证据均受时间校验 |
| P2 M1 origin invariance | PROVED_UNDER_ASSUMPTIONS | 同 B/config/cutoff 纯函数；前缀、两个新进程、低精度且 Inexact trap 环境均一致 |
| P2 M2 无状态 origin 要求 | COUNTEREXAMPLE_FOUND | 相同当前窗口，先前识别过与空历史得到不同记录集；额外输入不能隐藏 |
| P3 完整支持一致性 | PROVED_UNDER_ASSUMPTIONS | 两个 raw 谓词及负向 veto 只依赖同一有序 quartet；全局输入条件也须仍有效 |
| P4 分离／非平凡／歧义 | PROVED_UNDER_ASSUMPTIONS | E_j 要求 R_j，E_(j+1) 要求非 R_j；手算有有效反转且有合法拒绝 |
| P5 padding 与真实过期 | PROVED_UNDER_ASSUMPTIONS | 一条左移、无修订／插入、W>=2，双端仍 active 则 j-2 支持仍在；W0 原口径不成立 |
| P6 历史记录不变 | PROVED_UNDER_ASSUMPTIONS | append-only tuple 的归纳；不代表最新版本仍支持，也不修复当前结果丢失 |
| P7 H1 无条件滚动稳定性 | COUNTEREXAMPLE_FOUND | 原201条／13个旧事件重现，完整保存诊断精确一致 |
| P8 ATR seed 一致性误推 | COUNTEREXAMPLE_FOUND | 30,2,... 的原 EMA 手算；另给共同 TR 下的衰减／舍入上界，不推断离散几何连续 |
| P9 修订隔离／敏感性 | PROVED_UNDER_ASSUMPTIONS | 后可用版本不进入旧时点；相同端点但支持版本变更单独标记，不归于纯过期 |
| P10 小域实现一致性 | FINITE_DOMAIN_CHECKED | 19683/19683，独立整数公式对照 Decimal kernel；只覆盖声明域 |
| P11 下游与市场语义适用 | UNRESOLVED | 无交替保证、无真实日历／多股票验证，不启动完整引擎或006C-Q |

附带的有条件不相容论证仅针对“输出保留所有既往识别事实”同时“输出只由当前 B 决定且不声明历史”
这两个要求：同 B、不同合法历史的手例使二者矛盾。没有由 H1 单个反例推论所有有限内存算法不可能。

## 3. 原始指标与两个模型的实际对照

[合成证据](run-01/witnesses.json)独立复现完整 H1 保存诊断并比对 canonical 内容，匹配为 true。
OLD 活动 Pivot=0，BOTH=13；13个反转完成时间均早于 OLD cutoff，极值及确认引用仍 active。
保存各事件两端引用、极值时间、反转时间和本双 cutoff 观察日程的首次出现时刻，不能称为新确认。

R02 四条行情陷阱及镜像保持：baseline 只输出首个 Pivot；H1 第二个极值为 LOW95／HIGH105，
真实更极端的 LOW90／HIGH110 被允许域排除。ATR 单元证据明确标为 constant-ATR diagnostic。
15条 ATR 手例保留 old ATR13=4、old ATR14=3.733333333333333333、shifted ATR13=2。
既有六个 AVGO 微小几何漂移及原0.5 ATR/material 标记只引用旧报告，不重跑、不覆盖、不改容差。

[枚举](run-01/enumeration.json)：全体 3^9=19683 个九条 close 单词，close∈{99,100,101}，
O=C、H=C+1、L=C-1，前8和后8比较，无随机 seed；实际0.411秒，预算120秒，完整完成。
每个窗口通过独立整数局部转折公式核对；同一数据分别展示 W0 与 W2，不声称为两个独立市场试验。

| 视图 | 原双端机会 | 丢失 | 重现 | 真实 active 过期 | 完整支持机会 | 完整支持丢失 | 被收窄口径排除 |
|---|---:|---:|---:|---:|---:|---:|---:|
| M1 W0 | 17010 | **3402** | 0 | 0 | 13608 | 0 | **3402（20%）** |
| M1 W2 | 13608 | 0 | 0 | 3402 | 13608 | 0 | 0 |

W0 的 hand center2 左移后双端仍有效、完整支持丢失，**明确计作丢失，不改名为自然过期**。
W2 是公开改变 active 左界后的另一个视图，此时相同事件才真正退出 active。N8 的两条 padding
牺牲25%的 active 条目容量，不是声称损失25%的真实市场事件。两套分母及原始标记完整保留。
M2 的旧记录在两种视图都保留，但当前 M1 的这些丢失指标不因此归零。
收窄完整支持条件后的零丢失，不是原 Q/H1 通过，也不能靠全体 UNCERTAIN 解释为成功。

## 4. 验证与文件保护

环境：Windows、Python3.12.14、pytest8.4.1；沿用已声明依赖环境，无配置／依赖变更。
[执行命令、完整输出与退出码](validation.json)，[复现指南](README.md)。

| 检查 | 实际结果 |
|---|---|
| 既有132项研究 + 新36项R03 | **168 passed, 1 warning in 35.39s**；无 skips/xfails |
| Ruff | PASS，5个新增Python文件 |
| format check | PASS，5个文件 |
| strict mypy native / win32 | 均PASS，5个文件；native环境本身是Windows，不冒称Linux运行 |
| 原465个文件 mode/type/blob | 逐一与精确开发基线相同 |
| 合同及冻结计划 | 两个额外文件 mode/type/blob 相同；总保护467项 |
| 允许新增路径、链接、diff | 由R03独立 verifier 核验；最终记录 `protection-final.json` |

唯一 warning 是既有 Starlette/AnyIO BlockingPortal alias 弃用。没有跳过失败断言或通过批量改质量标志
掩盖问题。新增验证包含 public evaluate/JSON 质量、未来坏 payload、迟到／缺失版本、未完成、支持负向
证据修订、所有使用条目的可用时间、精确阈值、镜像、同条禁止、真实过期和相同端点不同支持版本。
原132项同时继续覆盖原四臂时间隔离、原参数与边界；没有替代或改写原测试。

本轮仅新增：

- `docs/research/PAQS_Q_CONFIRMATION_AND_ROLLING_STABILITY_R03.md`；
- `tools/research/paqs_q/r03/{__init__,models,witnesses,verify}.py`；
- `tests/research/paqs_q/r03/test_semantics.py`；
- 本 `research-03/` 下的冻结 PLAN、REPORT、README、合成JSON及验证／保护元数据。

产品src、原有业务及研究测试、原 Q/H1 参数／模型／证据、合同、数学与报告、冻结 universe、依赖配置、
PAQS-E、数据库迁移全部保持原 blob。没有修改旧 verifier 的允许清单。
没有数据库读取或写入，也没有重新计算用户数据库哈希；因此不冒称本轮作了外部数据库字节比对。
其他 worktree、未跟踪文件及 phase1_remediation_commit.txt 未操作。

按合同第6节，纯新增研究没有改变产品依赖，不运行无关业务／浏览器／Uvicorn／数据库套件，也不冒称通过。
本轮没有 OpenD、行情获取、凭据检查、网络研究请求、用户数据扫描或原始 AVGO 重跑。

## 5. 合同门槛与唯一后续建议

| Gate | 交付证据 |
|---|---|
| R03-01 | 规格第1节与 M1/M2 类型、真实 recognition 时间断言 |
| R03-02 | 全支持依赖、原双端指标、20%排除及W2容量代价并列 |
| R03-03 | 精确13事件诊断、四条陷阱／镜像、ATR seed反例 |
| R03-04 | 两个版本化最小模型、完整转移／域／代价表、手算接受与拒绝 |
| R03-05 | P1–P11量词／前提／证明／反例登记；有限枚举不称普遍定理 |
| R03-06 | 新36项及原132项保护质量／时间／Decimal／活动证据与合法不确定性 |
| R03-07 | 独立 verifier、465+2保护、冻结提交、链接／diff、可复現命令 |
| R03-08 | 以下单一建议；真实INCOMPLETE，采纳NOT AUTHORIZED |

**唯一建议：**独立审查后，若另行明确批准语义修订，再开展“有限局部确认凭证＋足够左侧支持”的
独立实验；历史识别记录保持独立审计概念，不把 M2 偷换为无状态候选。最小待决语义是：是否允许
“confirmed Pivot”采用非全局交替、带局部 veto／局部波动尺度的证书，并接受其覆盖代价。

下一份授权合同须先冻结真实 session/calendar 下的证据支持和候选域；继续原丢失／重现指标与排除分母，
以手算有效反转、双向／tie／gap／revision拒绝、同支持一致性和真实识别时钟为门槛。
拒绝无法解释的仍活动事件变化、隐瞒更宽依赖、识别时间回填、全体不输出或仅以降低UNCERTAIN为成功。
下游 Swing／Regime 的有效性和独立多股票完整性仍须分别检验，本轮不实施这些后续工作。

四项结论分别为：研究交付**实现与验证完成，待独立审查**；数学可行性**仅在已列假设下成立**；
真实行情完整性 **INCOMPLETE**（沿用 AVGO-only 缺口，未新增行情）；产品采纳 **NOT AUTHORIZED**。
只正常推送 R03 任务分支并读回精确最终 SHA、报告与冻结计划血缘。没有合并、接入产品或启动006C-Q。
