# R04 frozen experiment plan

Freeze before candidate comparisons. Base cc909077a470f7a6e717787307e8a4a8062ff792;
contract9a9113fb9a0ced3f7a481d833be4033d29016fea; authority8bf55f1ea5e7b3e9e65517ca9b5a505086bb761f.
R03-F01 closure1f8eba9b7112695e59e2dcd1bbef218dac51e74e was read via pinned GitHub API.
Only R04 additions; all482 base files plus issued contract protected. No prior verifier edits.

## Fixed model and source policy

The [candidate/calendar/quality specification](../../../research/PAQS_Q_LOCAL_CERTIFICATE_MARKET_APPLICABILITY_R04.md)
is frozen with this plan. Exactly one R03 raw-rule extension; original W/A/N, coefficient1,
four prices plus ordered calendar/qualification support; no output-dependent tuning or second model.
Keep R03 M2 state clarification, H1 rejection, R03 W0 loss/W2 cost unchanged.

Inventory found original normalized AVGO only and one archive Capture. Read-only mode=ro,
query_only=ON archive queries and accepted normalizer reconstruction matched all three originals
before candidate evaluation. Raw W1 has312 observations (prepare can exclude noncomplete ones),
D1 1500, M30 273. Do not overwrite source bytes. External calendar export:
`D:/AI_Infra_Quant_Codex_v1/task006b-q-r04-source/capture-calendar.json`, SHA256
`46c652b3062e4c6aeb43454dbcff5d8deb5f348639766b9e444b21116ebf6b2d`.
Archive calendar has1501 positive rows, coverage_complete=false, retrieval2026-09-09T08:29:34.687072Z;
historical availability unknown. No global completeness upgrade. Known closure supplement and
source hashes are in [freeze.json](freeze.json); no absent weekday is inferred closed.

Original cutoffs: exact100 per frame copied from R02 freeze, including insufficient M30 cases;
all original40 members remain (24US/16HK). AVGO is development data. Additional ceiling fixed at
2026-09-10T04:00:41Z; selection would be last100 completed slots by that ceiling, same universe.
Existing additional usable equities:0. Market acquisition request budget **0** this experiment:
no accessible additional authorized data inventoried; retained R02 Stooq/Alpha/Yahoo restrictions
give no concrete changed availability. Do not repeat them or register/access paid services.
Calendar documentation lookup is separate, using the two primary URLs in the specification;
not price acquisition. OpenD historical budget0. Complete AVGO and synthetic work despite deficit.

## Analysis and case selection fixed in advance

Evaluate every scheduled cutoff in both OBSERVATIONAL and AS_OF modes; preserve failures and raw
versions. Recompute unchanged baseline/H1 on identical original raw input and reconcile their
retained decision hashes. Candidate calendar domain differences are explicit, not improvement.
Report all-center census and metrics specified in the frozen math, per frame/mode and segment.
Deduplicate verbose witness details in a catalog, retaining every cutoff's row references and
counts. Track actual research-run recognition time separately from scheduled first_seen cutoff.

For each available frame, select earliest scheduled valid OBSERVATIONAL cutoff containing each
category, and earliest center therein: accepted active pattern; second of repeated consecutive
same-kind active pair; unambiguous raw veto (prefer omitted-more-extreme if any at that earliest
cutoff); calendar/missing-support rejection. Zero category occurrences are recorded as zero.
Additionally show first example of every distinct unexplained active-change mechanism, if any.
Maximum20 charts; all failures remain machine-readable even if the visual cap is reached.
Each case uses at most40 original OHLC bars around its selected center, with no candle after the
scheduled cutoff. Mark selected quartet, extreme, confirmation, rejected centers and edges;
label cutoff/timezone/source/adjustment/quality/mode/rule. Provide actual visual-review rationale,
including unfavorable/ambiguous interpretation. No hindsight direction or performance labels.

Synthetic tests: US/HK calendars, weekend/holiday and exceptional closures, lunch/overnight,
DST/early-close, missing expected bars, calendar conflict/late/revised/unknown, W1 factual
completion, unfinished/extended buckets, negative-veto support loss/insertion, both modes,
F01/F02 all inputs, origin/context identity, ordered-support stability, expiry/ties/dual/repeated
kinds and recognition. Retain168 research cases; add dedicated R04 tests. No new sample counts
from synthetic tests. No market parameter grid or PnL objective.

## Validation and decision

Run all168+new research tests; input/calendar unit suite when accepted derivation helper consumed;
Ruff/format, strict mypy native+win32 on new Python; links/diff/new-path scope; all482 base and
contract/frozen-plan/spec/manifest mode/type/blob. Fresh exclusive evidence only. Measure total
study runtime/output bytes and bounded N/census sizes; no SLA. All historical evidence unchanged.
Reject a claimed stable/applicable result if the unchanged-support claim fails, unknown adjacency
is filled, recognition is backdated or failed opportunities vanish. High same-kind/veto/omission
cost must inform the recommendation even when conditional losses are zero; no after-the-fact
numeric acceptance threshold. Broad market completeness remains INCOMPLETE without independent
coverage. Deliver one explicit next recommendation, stop for review; no product/merge/006C-Q.
